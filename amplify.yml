version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build:ssg
  artifacts:
    baseDirectory: dist/brother-bear-landing/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
